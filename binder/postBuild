pip install --global-option=build_ext --global-option="-I$CONDA_PREFIX/include" cvxoptklu

git clone https://github.com/curent/cvxopt /tmp/cvxopt

cd /tmp/cvxopt

pip install --global-option=build_ext --global-option="-I$CONDA_PREFIX/include" .

rm -rf /tmp/cvxopt